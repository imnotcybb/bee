spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
mybatis:
  type-aliases-package: com.cybb.bee.model
  mapper-locations: classpath:mapper/*.xml
server:
  port: 8080
